## Production Setup

Set `VITE_API_BASE_URL` before building for production:

```bash
VITE_API_BASE_URL=https://your-api-domain.com/api
```

For local development, the app defaults to `http://localhost:5001/api`.

Use [`frontend/.env.example`](/Users/apple/SATC/frontend/.env.example) as a template.
